let fib = fn(){
    let cache = {(0): 0, (1): 1};
    fn fib(n) {
        if n < 0 {
            throw "n can't be negative";
        }
        if type_of(n) != 'int' {
            throw "n must be an integer";
        }
        let result = cache[n];
        if result == null {
            result = fib(n-2) + fib(n-1);
            cache[n] = result;
        }
        return result;
    }
    return fib;
}();

for _ in range(0,1000) {
    for i in range(0,92) {
        fib(i);
    }
}