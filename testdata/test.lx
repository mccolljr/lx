let fib = fn() {
    let cache = {(0): 0, (1): 1};
    let _fib = fn(n) {
        let result = cache[n];
        if result == null {
            result = _fib(n-2) + _fib(n-1);
            cache[n] = result;
        }
        return result;
    };
    return _fib;
} ();

for i in range(0,90) {
    print(fib(i));
}